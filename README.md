# BiblioCase - Библиотека кейсов

Платформа для обмена опытом и знаниями. Сохраняйте и делитесь своими профессиональными кейсами, опытом из бизнеса, спорта и других сфер жизни.

## Возможности

- 📚 Создание и публикация кейсов
- 🔍 Поиск по названию и тексту
- 🏷️ Фильтрация по категориям
- 👤 Регистрация и авторизация пользователей
- 📊 Статистика платформы
- 🖼️ Загрузка изображений к кейсам

## Технологии

- **Backend**: Python, Flask, SQLite
- **Frontend**: HTML, CSS, JavaScript
- **База данных**: SQLite

## Установка и запуск

### Требования

- Python 3.7 или выше
- pip (менеджер пакетов Python)

### Шаги установки

1. **Клонируйте репозиторий или скачайте проект**

2. **Установите зависимости**

```bash
pip install -r requirements.txt
```

3. **Запустите сервер**

```bash
python app.py
```

Сервер запустится на `http://localhost:5000`

4. **Откройте в браузере**

Откройте браузер и перейдите по адресу `http://localhost:5000`

## Структура проекта

```
bibliocase/
├── app.py                 # Flask backend приложение
├── requirements.txt        # Зависимости Python
├── bibliocase.db          # SQLite база данных (создается автоматически)
├── uploads/               # Папка для загруженных изображений
├── templates/             # HTML шаблоны Flask
│   ├── main.html         # Главная страница
│   ├── login.html        # Страница входа
│   ├── signup.html       # Страница регистрации
│   ├── collections.html  # Коллекция кейсов
│   ├── create-case.html  # Создание кейса
│   └── case-detail.html  # Просмотр кейса
└── static/               # Статические файлы
    └── css/              # CSS стили
        ├── default-settings.css
        ├── styles.css
        ├── login-signup.css
        ├── create-case.css
        └── case-detail.css
```

## API Endpoints

### Аутентификация

- `POST /api/register` - Регистрация нового пользователя
  ```json
  {
    "username": "user123",
    "password": "password123"
  }
  ```

- `POST /api/login` - Вход пользователя
  ```json
  {
    "username": "user123",
    "password": "password123"
  }
  ```

### Кейсы

- `GET /api/cases` - Получить все кейсы
  - Параметры запроса: `?search=текст&category=Категория`

- `GET /api/cases/<id>` - Получить конкретный кейс

- `POST /api/cases` - Создать новый кейс
  ```json
  {
    "title": "Название кейса",
    "category": "Бизнес",
    "content": "Текст кейса...",
    "image": "data:image/png;base64,...",
    "user_id": 1
  }
  ```

### Дополнительно

- `GET /api/categories` - Получить список всех категорий
- `GET /api/stats` - Получить статистику платформы

## База данных

База данных SQLite создается автоматически при первом запуске. Структура:

### Таблица `users`
- `id` - ID пользователя
- `username` - Имя пользователя (уникальное)
- `password_hash` - Хеш пароля
- `created_at` - Дата создания

### Таблица `cases`
- `id` - ID кейса
- `title` - Название кейса
- `category` - Категория
- `image_path` - Путь к изображению
- `content` - Текст кейса
- `user_id` - ID автора
- `created_at` - Дата создания

## Использование

1. **Регистрация**: Создайте аккаунт на странице регистрации
2. **Вход**: Войдите в систему
3. **Создание кейса**: Нажмите "Создать кейс" и заполните форму
4. **Просмотр**: Просматривайте кейсы в коллекциях, используйте поиск и фильтры
5. **Детали**: Нажмите на кейс для просмотра полной информации

## Примечания

- Изображения сохраняются в папке `uploads/`
- Пароли хранятся в виде SHA-256 хешей
- Для продакшена рекомендуется использовать более безопасное хеширование паролей (bcrypt)
- CORS настроен для работы с локальным фронтендом

## Разработка

Для разработки запустите сервер с флагом `debug=True` (уже настроено в `app.py`):

```python
app.run(debug=True, port=5000)
```

Это позволит видеть детальные ошибки и автоматически перезагружать сервер при изменениях.

## Лицензия

Проект создан для образовательных целей.

